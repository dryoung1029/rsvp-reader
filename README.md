# RSVP Reader

A mobile-friendly rapid serial visual presentation reader with URL import, adjustable reading speed, chunk size, pivot-letter display, saved progress, and a Railway-ready Express backend.

## Deploy

Railway build command:

```bash
npm run build
```

Railway start command:

```bash
npm run start
```

The server uses `process.env.PORT`, so it is Railway-compatible.
